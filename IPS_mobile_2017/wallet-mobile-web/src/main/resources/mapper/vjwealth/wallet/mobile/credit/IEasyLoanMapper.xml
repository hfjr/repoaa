<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zhuanyi.vjwealth.wallet.mobile.credit.server.mapper.IEasyLoanMapper">

     <select id="queryUserInfoByUserId" resultType="java.util.Map" parameterType="java.lang.String">
         SELECT
            real_name AS userName,
            phone
        FROM
            wj_user_info
        WHERE
            id = #{userId}
     </select>


        <insert id="easyLoanApplySave" parameterType="java.lang.String">
         
            insert into wj_easy_loan_info(
             user_id,
             user_name,
             phone,
             channel_type,
             house_place,
             house_place_desc,
             apply_amount,
             condition_code,
             condition_desc,
             address_detail,
             loan_product_id,
             house_type,
             house_type_desc,
             period,
             period_unit,
             create_date
            )
             
             values(
               #{userId},
               #{userName},
               #{phone},
               #{userChannelType},
               #{housePlace},
               #{housePlaceDesc},
               #{applyAmount},
               #{fundPeriod},
               #{fundPeriodDesc},
               #{addressDetail},
               #{loanProductId},
               #{houseType},
               #{houseTypeDesc},
               #{loanPeriod},
               #{periodUnit},
               now()
             )
        
        </insert>


    <select id="queryEasyLoanInitParam" resultType="com.zhuanyi.vjwealth.wallet.mobile.credit.server.dto.easyLoan.EasyLoanInitDTO">
        select
            (select param_value from comm_params where param_key = 'easy_loan_top_pic_url' and param_group = 'easy_loan') as 'topIntroducePic',
            (select param_value from comm_params where param_key = 'easy_loan_down_pic_url' and param_group = 'easy_loan') as 'contactPhonePic',
            (select param_value from comm_params where param_key = 'easy_loan_tel_no' and param_group = 'easy_loan') as 'contactPhone'
            from DUAL
    </select>



	
</mapper>